// Este é um exemplo e deve ser preenchido com a lógica da câmera
export const useCamera = () => {
  const openCamera = () => {
    // Adicione a lógica para abrir a câmera e capturar imagens
  }

  return { openCamera }
}
