apiVersion: v1
kind: Service
metadata:
  name: api-chat-d-service
  namespace: ptcc-development
  labels:
    app: api-chat-d
    project: "ProntClinic"
    environment: "development"
spec:
  selector:
    app: api-chat-d
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
