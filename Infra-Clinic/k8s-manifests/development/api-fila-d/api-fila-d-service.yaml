apiVersion: v1
kind: Service
metadata:
  name: api-fila-d-service
  namespace: ptcc-development
  labels:
    app: api-fila-d
    project: "ProntClinic"
    environment: "development"
spec:
  selector:
    app: api-fila-d
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
